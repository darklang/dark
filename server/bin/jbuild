(jbuild_version 1)

(executable
  (
    (name main)
    (public_name main)
    (modules main)
    (modes (native))
    (flags (-warn-error (+A)))
    (libraries (dark))
  )
)

(executable
  (
    (name darkfile_to_rpc)
    (public_name darkfile_to_rpc)
    (modules darkfile_to_rpc)
    (modes (native))
    (flags (-warn-error (+A)))
    (libraries (dark))
  )
)

(executable
  (
    (name darkfile_bin_to_json)
    (public_name darkfile_bin_to_json)
    (modules darkfile_bin_to_json)
    (modes (native))
    (flags (-warn-error (+A)))
    (libraries (dark))
  )
)

(executable
  (
    (name darkfile_json_to_bin)
    (public_name darkfile_json_to_bin)
    (modules darkfile_json_to_bin)
    (modes (native))
    (flags (-warn-error (+A)))
    (libraries (dark))
  )
)
