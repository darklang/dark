(jbuild_version 1)

(library
  (
    (name dark)
    (public_name dark)
    (modes (byte))
    ;;; using ppx_jane causes problems
    (preprocess (pps (ppx_deriving ppx_fun ppx_pipebang -no-check)))
    (wrapped false)
    (libraries (core cohttp.lwt yojson threads postgresql))
  )
)