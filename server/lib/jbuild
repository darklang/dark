(jbuild_version 1)

(library
  (
    (name dark)
    (public_name dark)
    (modes (byte))
    ;;; using ppx_jane causes problems
    (preprocess (pps (ppx_jane ppx_pipebang ppx_deriving.show ppx_deriving.eq lwt.ppx)))
    (wrapped false)
    (libraries (core cohttp.lwt yojson threads postgresql ppx_derivers))
  )
)