(jbuild_version 1)

(library
  (
    (name libservice)
    (modes (native))
    (flags (-warn-error (+A)))
    (preprocess (pps (lwt.ppx)))
    (libraries (libcommon core batteries lwt yojson curl curl.lwt))
  )
)


