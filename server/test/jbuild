(jbuild_version 1)

(executable
  (
    (name test)
    (public_name test)
    (modes (byte))
    (preprocess (pps (ppx_jane ppx_fun)))
    (libraries (dark oUnit))
  )
)

(alias
 ((name    runtest)
  (deps    (test.bc))
  (action  (chdir ../../../
              (run ${<})))))