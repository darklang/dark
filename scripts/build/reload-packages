#!/usr/bin/env bash
. ./scripts/devcontainer/_assert-in-container "$0" "$@"

set -euo pipefail

TEST=false
PUBLISHED_FLAG=

for i in "$@"
do
  case "${i}" in
    --test)
    TEST=true
    shift
    ;;
    --published)
    PUBLISHED_FLAG=$i
    ;;
  esac
done

if [[ "$TEST" == "true" ]]; then
  LOG_PACKAGES="${DARK_CONFIG_RUNDIR}/logs/test-packages.log"
else
  LOG_PACKAGES="${DARK_CONFIG_RUNDIR}/logs/packages.log"
fi


grey="\033[1;30m"
reset="\033[0m"

echo -e "Reloading packages ${grey}(${LOG_PACKAGES})${reset}"
./scripts/run-local-exec $PUBLISHED_FLAG reload-packages > $LOG_PACKAGES 2>&1
echo "Done reloading packages"