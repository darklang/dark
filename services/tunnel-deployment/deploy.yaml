k8s:
  manually-deployed-config:
    configs:
      - isolate-tunnel.yaml
      - tunnel-service.yaml
  deployment:
    config-template: tunnel-deployment.template.yaml
    containers:
      - tunnel
    expected-args:
      - CHANGE_CAUSE
