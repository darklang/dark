k8s:
  manually-deployed:
    configs:
      - isolate-tunnel.yaml
      - tunnel-service.yaml
  release:
    config-template: tunnel-deployment.template.yaml
    containers:
      - tunnel
    expected-args:
      - CHANGE_CAUSE
