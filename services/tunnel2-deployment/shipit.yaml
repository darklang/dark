k8s:
  manually-deployed:
    configs:
      - access-only-tunnel2.yaml
      - tunnel2-service.yaml
      - tunnel2-network-policy.yaml
  release:
    config-template: tunnel2-deployment.template.yaml
    containers:
      - tunnel
    expected-args:
      - CHANGE_CAUSE
