apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: apiserver-network-policy
spec:
  podSelector:
    matchLabels:
      app: apiserver-app
  policyTypes:
    # egress policy is in tunnel2/access-only-tunnel2
    - Ingress
  ingress:
    - ports:
        - protocol: TCP
          port: http-proxy-port
