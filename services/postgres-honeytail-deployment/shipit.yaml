k8s:
  namespace: default
  manually-deployed:
    configs: []
  release:
    config-template: postgres-honeytail-deployment.template.yaml
    containers:
      - postgres-honeytail
    expected-args:
      - CHANGE_CAUSE
