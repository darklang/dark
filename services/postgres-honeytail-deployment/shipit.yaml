k8s:
  manually-deployed-config:
    configs: []
  deployment:
    config-template: postgres-honeytail-deployment.template.yaml
    containers:
      - postgres-honeytail
    expected-args:
      - CHANGE_CAUSE
