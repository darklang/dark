kind: Service
apiVersion: v1
metadata:
  name: darklang-nodeport
spec:
  type: NodePort
  selector:
    # FSTODO: this would point to apiserver-app when it's ready
    app: editor-app
  ports:
    - name: darklang-nodeport-port
      protocol: TCP
      port: 80
      targetPort: http-proxy-port
