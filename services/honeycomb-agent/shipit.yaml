k8s:
  manually-deployed-config:
    configs: []
    configmaps:
      - name: honeycomb-agent
        from-file:
          key: config.yaml
          filename: honeycomb-agent-config.yaml

    custom-post-apply:
      - kubectl rollout restart ds/honeycomb-agent
