This library provides helper functions _specific to Darklang_, wrapping around the
bindings for [tree-sitter](https://tree-sitter.github.io/tree-sitter/) provided by
the adjacent `LibTreeSitter` project.
