module Write =
  let testContents () : String = "Test conrents"

  (let filename = (File.createTemp ()) |> unwrap
   do (testContents ())
      |> String.toBytes
      |> (File.write filename)
   File.read filename) = PACKAGE.Darklang.Stdlib.Result.Result.Ok ((testContents ()) |> String.toBytes)
