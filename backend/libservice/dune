(library
  (name libservice)
  (modes native)
  (flags (-warn-error +A))
  (preprocess (pps lwt_ppx))
  (libraries  libcommon
              core
              lwt
              yojson
              curl
              curl.lwt
              cohttp.lwt
              postgresql
              )
)


