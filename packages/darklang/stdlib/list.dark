module Darklang =
  module Stdlib =
    module List =

      let dropLast (list: List<'a>) : List<'a> =
        match list with
        | [] -> []
        | head :: tail ->
          match tail with
          | [] -> []
          | _ -> List.append_v0 (List.singleton_v0 head) (PACKAGE.Darklang.Stdlib.List.dropLast tail)

