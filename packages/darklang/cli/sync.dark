module Darklang.Cli.Sync

// `dark sync` — keep your packages in step with your other instances, over HTTP. Local-first: sync is opt-in,
// and once you connect a peer, `dark sync daemon start` keeps things current in the background.
// Peers are URLs (running instances), never files — sync exchanges ops, not databases.

// How to describe the instances a sync actually got through to. When some peer DIDN'T answer, this says
// "1 of 2 instances" — because the alternative reads as a contradiction: a "Couldn't reach <peer>" line
// directly above a bare "Pulled from 1 instance" looks like one instance that both failed and worked, when
// it's two instances, one of each. Naming the reached peer instead would be honest too, but they're raw IPs
// until instances get names.
let reachedPhrase (reached: Int) (tried: Int) : String =
  if reached == tried then
    Text.plural reached "instance"
  else
    $"{Stdlib.Int.toString reached} of {Text.plural tried "instance"}"

// Say what a pull actually DID, in human terms — ONE LINE PER NAME. A raw op count is a lie by omission:
// authoring one value emits both a content op and its name-binding, so "5 changes" for one edit is nonsense.
// And a conflicting pull can re-assert the same name, so the same (action, kind, name) can arrive more than
// once — dedupe or it prints twice. A handful: one line each, because that's what you want to know. Many:
// counts grouped by action + kind ("10 new fns"), because a wall of names is worse than a summary.
let changeLines (changes: List<Darklang.Sync.EventLog.Change>) : List<String> =
  let named =
    changes
    |> Stdlib.List.map (fun c -> $"{c.action} {c.itemKind} {c.location}")
    |> Stdlib.List.unique

  if Stdlib.List.length named <= 6 then
    named
  else
    changes
    |> Stdlib.List.uniqueBy (fun c -> $"{c.action} {c.itemKind} {c.location}")
    |> Stdlib.List.groupByWithKey (fun c -> (c.action, c.itemKind))
    |> Stdlib.List.map (fun group ->
      let (key, cs) = group
      let (action, kind) = key
      let n = Stdlib.List.length cs
      let kindWord = if n == 1 then kind else Stdlib.String.pluralize kind
      $"{Stdlib.Int.toString n} {action} {kindWord}")

// Print "Pulled from {who}: …" — inline for a single name (a tight one-liner), one indented line each for a
// few, grouped counts for many. `changeLines` already deduped and chose the form.
let printPull (who: String) (changes: List<Darklang.Sync.EventLog.Change>) : Unit =
  match changeLines changes with
  | [ one ] -> Stdlib.printLine (Stdlib.Cli.UI.Colors.success $"Pulled from {who}: {one}")
  | many ->
    Stdlib.printLine (Stdlib.Cli.UI.Colors.success $"Pulled from {who}:")

    many
    |> Stdlib.List.iter (fun l -> Stdlib.printLine (Stdlib.Cli.UI.Colors.success $"  {l}"))

// After a pull, mention any name where an incoming change overwrote a different local one — so last-writer-wins
// is never SILENT, without crying "conflict". Most of the time this is just a peer that was ahead of you (a
// plain catch-up, nothing to resolve); occasionally it's a genuine two-sided edit. Either way LWW already
// picked, so this is a quiet FYI you can review, not an alarm. (Telling the two apart needs op lineage we don't
// track yet — see the known-issue note; until then we don't call a catch-up a conflict.)
let conflictNudge () : Unit =
  let unreviewed =
    Darklang.Sync.Conflicts.list ()
    |> Stdlib.List.filter (fun c -> c.status == "auto-resolved")
    |> Stdlib.List.length

  if unreviewed > 0 then
    Stdlib.printLine (
      Stdlib.Cli.UI.Colors.dimText
        $"  {Text.plural unreviewed "name"} updated by a peer (last-writer-wins) — `dark conflicts` to review")
  else
    ()

// Serve the sync endpoint so peers can pull from you. Blocks until Ctrl+C (foreground); for boot, use
// `dark sync daemon start`. Same router `dark serve Darklang.Sync.Server.router` would run — just less
// to type, and it prints the address peers actually need.
let serveOn (port: Int) (state: AppState) : AppState =
  let portStr = Stdlib.Int.toString port

  let announce () : Unit =
    [ Stdlib.Cli.UI.Colors.success $"Serving sync on http://localhost:{portStr} — peers can pull from you here."
      Stdlib.Cli.UI.Colors.hint $"  Remote peers use your tailnet address, e.g. http://you.tailnet:{portStr}"
      Stdlib.Cli.UI.Colors.hint "  Each peer's request prints here as it arrives."
      Stdlib.Cli.UI.Colors.hint "  Keep it running (serve on boot):  dark sync daemon start"
      "  Press Ctrl+C to stop." ]
    |> Stdlib.printLines

  match
    Stdlib.HttpServer.serve
      (Stdlib.HttpServer.Config.defaults port)
      Darklang.Sync.Server.router
      announce
  with
  | Ok _ -> ()
  | Error msg -> Stdlib.printLine (Stdlib.Cli.UI.Colors.error msg)

  state

// ── `dark sync daemon` — automatic sync: ONE toggle over both the pull daemon (`sync-pull`) and the serve
// daemon (`sync-serve`). Settings live in cli-config (mode / interval / port / on-boot), adjustable via
// `dark sync daemon config`. Replaces `dark apps enable sync` as the blessed surface.

let daemonMode () : String =
  match Darklang.Cli.Config.get "sync.daemon.mode" with
  | Some m -> m
  | None -> "pull+serve"

let daemonSlugs (mode: String) : List<String> =
  match mode with
  | "pull-only" -> [ "sync-pull" ]
  | "serve-only" -> [ "sync-serve" ]
  | _ -> [ "sync-pull", "sync-serve" ]

// The default comes from the daemon itself, not a copy: this is what `dark sync status` REPORTS, and a
// second hardcoded number here would eventually disagree with the one the daemon actually sleeps on.
let intervalSec () : Int =
  (Darklang.Cli.Config.getInt
    "apps.sync-pull.intervalMs"
    Darklang.Sync.defaultPollIntervalMs)
  / 1000

let servePort () : Int =
  Darklang.Cli.Config.getInt "apps.sync-serve.port" 9000

let onBoot () : Bool =
  match Darklang.Cli.Config.get "sync.daemon.onBoot" with
  | Some "no" -> false
  | _ -> true

let settingsLine () : String =
  let boot = if onBoot () then "on boot" else "not on boot"

  $"mode: {daemonMode ()}, every {Stdlib.Int.toString (intervalSec ())}s, port {Stdlib.Int.toString (servePort ())}, {boot}"

let setConfig (key: String) (value: String) : Unit =
  let updated =
    Stdlib.Dict.setOverridingDuplicates (Darklang.Cli.Config.readConfig ()) key value

  let _ = Darklang.Cli.Config.writeConfig updated
  ()

// The first-run dialog: explain the defaults, offer to adjust them inline. In a non-interactive context
// (piped/no TTY) the "Adjust?" prompt reads EOF → No, so it falls through to defaults without blocking.
let firstRunDialog () : Unit =
  Stdlib.printLine "Setting up automatic sync. Defaults:"

  Stdlib.printLine (
    Stdlib.Cli.UI.Colors.hint
      $"  - pull from peers + serve to them, every {Stdlib.Int.toString (intervalSec ())}s")

  Stdlib.printLine (
    Stdlib.Cli.UI.Colors.hint
      $"  - serve on port {Stdlib.Int.toString (servePort ())} — all branches, open to anyone who can reach you")

  Stdlib.printLine (Stdlib.Cli.UI.Colors.hint "  - start automatically on boot")

  let _ =
    if Stdlib.Cli.UI.Prompt.confirm "Adjust these settings?" then
      let intervalIn =
        Stdlib.Cli.UI.Prompt.ask
          $"  interval seconds [{Stdlib.Int.toString (intervalSec ())}]:"

      // Validate through daemonConfigValue (the same range checks `dark sync daemon config` enforces) so the
      // first-run dialog can't store an out-of-range interval/port that only fails later at daemon start.
      let _ =
        match daemonConfigValue "interval" intervalIn with
        | Some((k, v)) -> setConfig k v
        | None -> ()

      let portIn =
        Stdlib.Cli.UI.Prompt.ask $"  port [{Stdlib.Int.toString (servePort ())}]:"

      let _ =
        match daemonConfigValue "port" portIn with
        | Some((k, v)) -> setConfig k v
        | None -> ()

      let modeIn =
        Stdlib.Cli.UI.Prompt.select
          "  mode:"
          [ "pull+serve", "pull-only", "serve-only" ]

      let _ = setConfig "sync.daemon.mode" modeIn
      let boot = Stdlib.Cli.UI.Prompt.confirm "  start on boot?"
      setConfig "sync.daemon.onBoot" (if boot then "yes" else "no")
    else
      ()

  setConfig "sync.daemon.configured" "yes"
  // Separate the dialog from the "Sync started" lines the caller prints next — otherwise a non-interactive
  // run (prompt auto-answered "no") runs the "Adjust these settings?" line straight into "✓ Sync started".
  Stdlib.printLine ""

let daemonStart (state: AppState) : AppState =
  let firstRun =
    match Darklang.Cli.Config.get "sync.daemon.configured" with
    | Some "yes" -> false
    | _ -> true

  let _ =
    if firstRun then
      firstRunDialog ()
    else
      Stdlib.printLine (
        Stdlib.Cli.UI.Colors.success $"Automatic sync started — {settingsLine ()}.")

  let started =
    Stdlib.List.fold (daemonSlugs (daemonMode ())) state (fun s slug ->
      Darklang.Cli.Apps.Command.startDaemon s slug)

  // A daemon has no terminal, so everything it says — pulls, served requests, and any startup error — goes to
  // its log instead of to you. Without this you'd have no reason to think a log existed, and the daemon's the
  // case where you most need one: it's the mode where nothing is printed at you.
  let _ =
    daemonSlugs (daemonMode ())
    |> Stdlib.List.map (fun slug -> Stdlib.Cli.UI.Colors.hint $"  Watch it:  dark apps logs {slug}")
    |> Stdlib.printLines

  started

let daemonStop (state: AppState) : AppState =
  let _ =
    Stdlib.List.map [ "sync-pull", "sync-serve" ] (fun slug ->
      match Stdlib.Cli.Daemon.stop slug with
      | Ok _ -> Stdlib.printLine (Stdlib.Cli.UI.Colors.success $"Stopped {slug}.")
      | Error _ -> ())

  Stdlib.printLine (
    Stdlib.Cli.UI.Colors.hint
      "Automatic sync off. One-off: dark sync (pull), or dark sync serve (be reachable).")

  state

let daemonStatusCmd (state: AppState) : AppState =
  Stdlib.printLine "Automatic sync:"
  Stdlib.printLine $"  {Stdlib.Cli.Daemon.statusLine "sync-pull"}"
  Stdlib.printLine $"  {Stdlib.Cli.Daemon.statusLine "sync-serve"}"
  Stdlib.printLine (Stdlib.Cli.UI.Colors.hint $"  {settingsLine ()}    (change: dark sync daemon config)")
  state

let daemonConfigShow (state: AppState) : AppState =
  Stdlib.printLine "Automatic sync settings:"
  Stdlib.printLine $"  mode      {daemonMode ()}   (pull+serve | pull-only | serve-only)"
  Stdlib.printLine $"  interval  {Stdlib.Int.toString (intervalSec ())}s"
  Stdlib.printLine $"  port      {Stdlib.Int.toString (servePort ())}"
  Stdlib.printLine $"  on-boot   {if onBoot () then "yes" else "no"}"

  Stdlib.printLine (
    Stdlib.Cli.UI.Colors.hint
      "  change: dark sync daemon config <mode|interval|port|on-boot> <value>")

  state

/// Validate a daemon config `(key, value)` and map it to its stored `(config-key, store-value)`, or None if
/// the value is invalid. Pure + exported so the validation is directly testable. An invalid value → None → the
/// caller reports an error instead of storing junk that silently reverts to a default (a `port abc` or
/// `on-boot false` that looks like it took but doesn't).
let daemonConfigValue
  (key: String)
  (value: String)
  : Stdlib.Option.Option<(String * String)> =
  match key with
  | "mode" ->
    if Stdlib.List.member_v0 [ "pull+serve", "pull-only", "serve-only" ] value then
      Stdlib.Option.Option.Some(("sync.daemon.mode", value))
    else
      Stdlib.Option.Option.None
  | "port" ->
    match Stdlib.Int.parse value with
    | Ok p ->
      if p > 0 && p <= 65535 then
        Stdlib.Option.Option.Some(("apps.sync-serve.port", value))
      else
        Stdlib.Option.Option.None
    | Error _ -> Stdlib.Option.Option.None
  | "on-boot" ->
    if Stdlib.List.member_v0 [ "yes", "no" ] value then
      Stdlib.Option.Option.Some(("sync.daemon.onBoot", value))
    else
      Stdlib.Option.Option.None
  | "interval" ->
    match Stdlib.Int.parse value with
    | Ok secs ->
      // reject non-positive — a 0/negative interval would make the daemon busy-loop (see nextPollMs clamp).
      if secs > 0 then
        Stdlib.Option.Option.Some(
          ("apps.sync-pull.intervalMs", Stdlib.Int.toString (secs * 1000))
        )
      else
        Stdlib.Option.Option.None
    | Error _ -> Stdlib.Option.Option.None
  | _ -> Stdlib.Option.Option.None

let daemonConfigSet
  (state: AppState)
  (key: String)
  (value: String)
  : AppState =
  let stored = daemonConfigValue key value

  let _ =
    match stored with
    | Some((configKey, storeValue)) ->
      let updated =
        Stdlib.Dict.setOverridingDuplicates
          (Darklang.Cli.Config.readConfig ())
          configKey
          storeValue

      let _ = Darklang.Cli.Config.writeConfig updated

      Stdlib.printLine (
        Stdlib.Cli.UI.Colors.success
          $"Set {key} = {value}. Apply: dark sync daemon stop, then dark sync daemon start.")
    | None ->
      Stdlib.printLine (
        Stdlib.Cli.UI.Colors.error
          $"Can't set '{key}' to '{value}'. Keys: mode | interval (seconds) | port | on-boot (yes/no).")

  state

let help (_state: AppState) : String =
  [ "dark sync — keep your packages in step with your other instances, over HTTP."
    "Sync is opt-in, and only between the machines you want. Once you connect a peer it stays current."
    ""
    "  dark sync                    sync now from every instance you're connected to"
    "  dark sync connect <url>      remember an instance, and sync with it from now on"
    "  dark sync disconnect <url>   forget an instance, and stop syncing with it"
    "  dark sync serve              let peers pull from you (add --port N; default 9000)"
    "  dark sync status             who you're syncing with, and how current each is"
    "  dark sync fetch <name|url>   sync once from one instance (by its name or URL)"
    "  dark sync --from <name>      same, by the name shown in `dark sync status`"
    ""
    "`connect` only records an instance on this machine — it sends nothing and changes nothing on theirs."
    "After that, `dark sync` reads from every instance you've recorded; `fetch` reads from just the one."
    ""
    "All your branches sync wholesale — a branch is the same on every instance you connect."
    ""
    "Automatic (background, and across reboots):"
    "  dark sync daemon start       pull from + serve to your peers on an interval"
    "  dark sync daemon status      whether it's running, and with what settings"
    "  dark sync daemon config      view/adjust interval, port, mode, on-boot"
    "  dark sync daemon stop        turn automatic sync off"
    ""
    "When two instances edit the same name, sync auto-resolves (latest wins) but records it:"
    "  dark conflicts               review + override sync divergences" ]
  |> Stdlib.String.join "\n"


let execute (state: AppState) (args: List<String>) : AppState =
  match args with
  | [] ->
    // bare `dark sync` = pull now from every connected instance (the daemon automates this).
    match Darklang.Sync.peers () with
    | [] ->
      Stdlib.printLine "You're not syncing with anyone yet."

      Stdlib.printLine (
        Stdlib.Cli.UI.Colors.hint "  dark sync connect <url>   sync with one of your instances")

      state
    | ps ->
      let results = Stdlib.List.map ps (fun p -> Darklang.Sync.pull p)

      let pulled =
        Stdlib.List.fold results 0 (fun acc r ->
          match r with
          | Ok result -> acc + result.applied
          | Error _ -> acc)

      let allChanges =
        Stdlib.List.fold results [] (fun acc r ->
          match r with
          | Ok result -> Stdlib.List.append acc result.changes
          | Error _ -> acc)

      // Each pull error is already peer-prefixed and self-describing — a genuine
      // "Couldn't reach {peer}" reads differently from a "{peer}: a database error
      // applying ops …". Surface the real message instead of bucketing every
      // failure as "unreachable" (which hides apply/DB errors behind a network lie).
      let errors =
        Stdlib.List.filterMap results (fun r ->
          match r with
          | Ok _ -> Stdlib.Option.Option.None
          | Error e -> Stdlib.Option.Option.Some e)

      let reached = (Stdlib.List.length ps) - (Stdlib.List.length errors)

      let _ =
        errors
        |> Stdlib.List.map (fun e -> Stdlib.Cli.UI.Colors.error e)
        |> Stdlib.printLines

      if Stdlib.List.length allChanges > 0 then
        printPull (reachedPhrase reached (Stdlib.List.length ps)) allChanges
      else if pulled > 0 then
        // Ops applied, but nothing a human would call a change — branch structure, or content whose
        // name-binding came in an earlier batch. Say that honestly rather than invent a count.
        Stdlib.printLine (
          Stdlib.Cli.UI.Colors.success
            $"Synced with {reachedPhrase reached (Stdlib.List.length ps)} — no new names")
      else if reached > 0 then
        Stdlib.printLine (
          Stdlib.Cli.UI.Colors.success
            $"Already up to date with {reachedPhrase reached (Stdlib.List.length ps)}")
      else
        ()

      let _ = conflictNudge ()
      state

  | [ "status" ] ->
    match Darklang.Sync.status () with
    | [] ->
      Stdlib.printLine "You're not syncing with anyone yet."

      Stdlib.printLine (
        Stdlib.Cli.UI.Colors.hint "  dark sync connect <url>   sync with one of your instances")

      state
    | sts ->
      // Frame the relationship as two-way: sync is pull-based, so you pull from them AND they pull your
      // commits from you (that's how your work reaches them — there's no push).
      let header =
        $"Syncing with {Text.plural (Stdlib.List.length sts) "instance"} (you pull with `dark sync`; they pull your commits from you):"

      // Width across the derived NAMES (what leads each row), not the URLs — so columns align on the handle
      // you actually use.
      let width =
        Stdlib.List.fold sts 0 (fun acc pc ->
          let len =
            Stdlib.String.length (Darklang.Sync.instanceName (Stdlib.Tuple2.first pc))

          if len > acc then len else acc)

      let lines =
        Stdlib.List.map sts (fun pc ->
          let peer = Stdlib.Tuple2.first pc
          let cursor = Stdlib.Tuple2.second pc
          let name = Darklang.Sync.instanceName peer

          let padded = Stdlib.String.padEndToWidth name width

          // Name leads (you sync with `feriel`, not a URL); the URL trails, dimmed, for when you need it.
          // Cursor is the PEER's rowid — meaningless to show; what's honest is whether we've pulled it yet.
          let mark = if cursor == 0L then "not pulled yet" else "pulled"
          $"  {padded}   {Stdlib.Cli.UI.Colors.dim}{peer}{Stdlib.Cli.UI.Colors.reset}   ({mark})")

      // Every branch syncs wholesale — the same branches exist on every instance you connect.
      let branchNames =
        Stdlib.List.map (Darklang.SCM.Branch.list ()) (fun b -> b.name)

      let branchLine =
        match branchNames with
        | [] -> []
        | names ->
          [ Stdlib.Cli.UI.Colors.hint
              $"  {Text.plural (Stdlib.List.length names) "branch"} syncing: {Stdlib.String.join names ", "}" ]

      Stdlib.printLines (
        Stdlib.List.append (Stdlib.List.append [ header ] lines) branchLine)

      state

  | [ "connect", peer ] ->
    match Darklang.Sync.connect peer with
    | Ok _ ->
      // Probe the peer now, so a wrong URL/port shows up here (when you can fix it) instead of on a later
      // silent `dark sync`. The peer is added either way — an offline instance will just sync once it's up.
      match Darklang.Sync.reachable peer with
      | Ok n ->
        Stdlib.printLine (
          Stdlib.Cli.UI.Colors.success $"Now syncing with {peer} — reachable ({Stdlib.Int64.toString n} ops)")
        Stdlib.printLine (
          Stdlib.Cli.UI.Colors.hint "  dark sync — sync now.  dark sync daemon start — keep current on boot.")
      | Error _ ->
        Stdlib.printLine (Stdlib.Cli.UI.Colors.success $"Added {peer}")
        Stdlib.printLine (
          Stdlib.Cli.UI.Colors.hint
            "  can't reach it yet — check the URL and that the instance is running `dark sync serve`; it'll sync once it's up")
    | Error e -> Stdlib.printLine (Stdlib.Cli.UI.Colors.error e)

    state

  | [ "fetch", peerRef ]
  | [ "--from", peerRef ] ->
    // A name (feriel) or a full URL. A bare name you're not syncing with is the common typo, so say so
    // rather than trying to pull from a non-URL.
    match Darklang.Sync.resolvePeer peerRef with
    | None ->
      Stdlib.printLine (
        Stdlib.Cli.UI.Colors.error
          $"Not syncing with an instance named '{peerRef}'. Use its URL, or `dark sync status` to see the names you have.")

      state
    | Some peer ->
      let _ =
        match Darklang.Sync.pull peer with
        | Ok result ->
          let who = Darklang.Sync.instanceName peer
          if Stdlib.List.length result.changes > 0 then
            printPull who result.changes
          else if result.applied > 0 then
            Stdlib.printLine (Stdlib.Cli.UI.Colors.success $"Synced with {who} — no new names")
          else
            Stdlib.printLine (Stdlib.Cli.UI.Colors.success $"Already up to date with {who}")
        | Error e -> Stdlib.printLine (Stdlib.Cli.UI.Colors.error e)

      let _ = conflictNudge ()
      state

  | [ "disconnect", peerRef ] ->
    // Disconnect by the name you see in `status`, or by URL. Resolve first so `disconnect feriel` works.
    let peer =
      match Darklang.Sync.resolvePeer peerRef with
      | Some u -> u
      | None -> peerRef

    if Darklang.Sync.disconnect peer then
      Stdlib.printLine (Stdlib.Cli.UI.Colors.success $"Stopped syncing with {Darklang.Sync.instanceName peer}")
    else
      Stdlib.printLine (Stdlib.Cli.UI.Colors.hint $"You weren't syncing with {peerRef}")

    state

  | [ "daemon" ]
  | [ "daemon", "status" ] -> daemonStatusCmd state
  | [ "daemon", "start" ] -> daemonStart state
  | [ "daemon", "stop" ] -> daemonStop state
  | [ "daemon", "config" ] -> daemonConfigShow state
  | [ "daemon", "config", key, value ] -> daemonConfigSet state key value

  | [ "serve" ] -> serveOn 9000 state
  | [ "serve", "--port", portStr ] ->
    match Stdlib.Int.parse portStr with
    | Ok p -> serveOn p state
    | Error _ ->
      Stdlib.printLine (Stdlib.Cli.UI.Colors.error $"Invalid --port: {portStr}")
      state

  | [ "help" ] ->
    Stdlib.printLine (help state)
    state
  | _ ->
    Stdlib.printLine (Stdlib.Cli.UI.Colors.error $"Unknown: dark sync {Stdlib.String.join args " "}")
    Stdlib.printLine ""
    Stdlib.printLine (help state)
    state


let complete
  (state: AppState)
  (args: List<String>)
  : List<Completion.CompletionItem> =
  match args with
  | [] ->
    [ "status", "connect", "disconnect", "serve", "fetch", "daemon" ]
    |> Stdlib.List.map Completion.simple
  | _ -> []
