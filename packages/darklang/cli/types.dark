module Darklang =
  module Cli =
    module Types =
      type Msg =
        | ProcessInput of String
        | Exit

      type AppState = 
        { isExiting: Bool
          lastCommand: String
          output: String }

      let initState () : AppState =
        AppState { isExiting = false; lastCommand = ""; output = "" }