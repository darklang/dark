{
  "name": "dark",
  "version": "1.0.0",
  "main": "main.js",
  "repository": "git@github.com:darklang/dark.git",
  "license": "UNLICENSED",
  "private": true,
  "scripts": {
    "build:bundle": "browserify server/src/main.js --standalone bundle -o server/static/bundle.js",
    "build:exe": "browserify server/src/darkex.js --standalone exe -o server/static/darkex.js",
    "build": "run-s build:bundle build:exe"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.3",
    "browserify": "^16.1.1"
  },
  "dependencies": {
    "domready": "^1.0.8",
    "sha2": "^1.0.2",
    "mouse-wheel": "^1.2.0",
    "rollbar": "^2.4.2"
  }
}
