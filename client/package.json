{
  "name": "client",
  "version": "0.1.0",
  "repository": "git@github.com:darklang/dark.git",
  "private": true,
  "author": "",
  "devDependencies": {
    "@glennsl/bs-jest": "^0.4.5",
    "browserify": "^16.1.1",
    "bs-platform": "4.0.7",
    "npm-run-all": "^4.1.3"
  },
  "dependencies": {
    "@glennsl/bs-json": "3.0.0",
    "bs-webapi": "^0.13.0",
    "bucklescript-tea": "^0.9.1",
    "domready": "^1.0.8",
    "mouse-wheel": "^1.2.0",
    "rollbar": "^2.4.2",
    "sha2": "^1.0.2"
  },
  "scripts": {
    "clean": "bsb -clean-world",
    "build:bsb": "bsb -make-world",
    "build:js": "browserify lib/js/src/App.bs.js --standalone app -o ../backend/static/app.js",
    "build:appsupport": "browserify src/appsupport.js --standalone appsupport -o ../backend/static/appsupport.js",
    "build:analysissupport": "browserify src/analysissupport.js --standalone analysissupport -o ../backend/static/analysissupport.js",
    "build": "run-s build:bsb build:js build:appsupport build:analysissupport",
    "test": "jest"
  }
}
